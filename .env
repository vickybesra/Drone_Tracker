# Tractor Tracker backend — environment variables
#
# IMPORTANT:
# - Do NOT commit this file to source control.
# - Place your Firebase Admin SDK key as serviceAccountKey.json in the project root.
# - server.js does not load .env automatically. To use these values:
#   a) export them in your shell before running `npm run server`, or
#   b) update docker-compose.yml to reference them (e.g., MQTT_USERNAME: ${MQTT_USERNAME}).
#
# Backend HTTP port
PORT=8080

# MQTT broker configuration
# For local broker started via Aedes or Mosquitto
MQTT_BROKER_URL=mqtt://localhost:1883

# If your broker requires auth, set these; otherwise leave blank
MQTT_USERNAME=myuser
MQTT_PASSWORD=1234

# Firebase Realtime Database URL for your project (leave blank to use server.js default)
# Examples:
# - Legacy: https://your-project-id-default-rtdb.firebaseio.com
# - Regional: https://your-project-id-<region>.firebasedatabase.app
#DATABASE_URL="https://your-project-id-default-rtdb.firebaseio.com"
DATABASE_URL=

# Notes:
# - The Firebase Admin SDK key is read from ./serviceAccountKey.json (file), not from env.
# - In docker-compose, the backend mounts ./serviceAccountKey.json to /app/serviceAccountKey.json (read-only).
